// JavaScript version - types are handled through JSDoc comments if needed

/**
 * @typedef {Object} LeetCodeProfile
 * @property {string} status
 * @property {string} message
 * @property {number} totalSolved
 * @property {number} easySolved
 * @property {number} mediumSolved
 * @property {number} hardSolved
 * @property {string} ranking
 * @property {number} contributionPoints
 * @property {number} reputation
 * @property {number} rating
 * @property {number} attendedContestsCount
 */

/**
 * @typedef {Object} LeetCodeData
 * @property {string} username
 * @property {LeetCodeProfile} profile
 */

// Export empty object since we don't have actual exports in JS version
export {};
